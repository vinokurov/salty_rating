from setuptools import setup

setup(
    name='Salty Rating',
    packages=['salty.rating'],
    include_package_data=True,
    # install_requires=[
    #     'pandas',
    #     'pyyaml',
    #     'numpy'
    # ],
)